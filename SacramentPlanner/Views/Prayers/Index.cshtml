@model IEnumerable<SacramentPlanner.Models.Prayer>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PrayerDate)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.FkPrayerTypeNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FkWardMember)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PrayerDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkPrayerTypeNavigation.TypePrayer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkWardMember.Fname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrayerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrayerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrayerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
