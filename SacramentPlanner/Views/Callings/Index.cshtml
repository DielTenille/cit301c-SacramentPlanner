@model IEnumerable<SacramentPlanner.Models.Calling>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Bishopric)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.OtherLeader)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CallingName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Bishopric)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OtherLeader)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CallingName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CallingId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CallingId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CallingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
